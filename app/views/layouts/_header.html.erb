<nav id="header" class="navbar navbar-custom">
    <div class="container-fluid">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#header-navbar">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="/">Snipbase</a>
        </div>
        <div id="header-navbar" class="navbar-collapse collapse" aria-expanded="false" style="">
            <ul class="nav navbar-nav navbar-right">
                <% if logged_in? %>
                    <li><%= link_to "Groups", show_user_groups_path(current_user.username), :id => "groups-link" %>
                    </li>
                    <li><%= link_to "+Snippet", add_path, :id => "add-snippet-link" %>
                    </li>

                    <li class="vertical-separator hidden-xs"><span>|</span>
                    <li>
                    <%= link_to current_user.username, show_user_path(current_user.username), :id => "user-profile-link" %>
                    </li>
                    <li>
                        <a id="notifications-toggle" data-placement="bottom" title="Notifications" href="#">N</a>
                    </li>
                    <li class="dropdown hidden-xs">
                        <a class="settings" data-toggle="dropdown" class="dropdown-toggle" href="#"><i class="fa fa-cog"></i><span class="caret"></span></a>
                        <ul class="dropdown-menu" role="menu">
                            <% if current_user.is_admin %>
                                <li>
                                    <%= link_to "Administration", admin_path %>
                                </li>
                            <% end %>
                            <li>
                                <%= link_to "Settings", settings_path %>
                            </li>
                            <li>
                                <%= link_to "Log out", logout_path, method: :post, class: "logout" %>
                            </li>
                        </ul>
                    </li>
                    <li class="visible-xs">
                        <%= link_to "Settings", settings_path %>
                    </li>
                    <li class="visible-xs">
                        <%= link_to "Log out", logout_path, method: :post, class: "logout" %>
                    </li>
                <% else %>
                    <li><a href="/public">Public Snippets</a>
                    <li><a href="/tour">Tour</a>
                    <li class="vertical-separator hidden-xs"><span>|</span>
                    <li class="no-block">
                        <%= link_to "Log in", login_path, class: "login" %>
                        <span class="faded">or</span>
                        <%= link_to "Sign up", register_path, class: "signup"%>
                    </li>
                <% end %>
            </ul>
        </div>
    </div>
    <%= render 'shared/notifications' %>
</nav>
